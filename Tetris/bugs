Tetris - 
Bugs:
rotation should adjust itself if it cannot rotate
there is a bug where a column is duplicated upwards. 
game is not as smooth as regular tetris

features"
may need to add queue to avoid mutikey inputs
show next piece
add function to drop piece
add function to hold piece
add clock for game duration
